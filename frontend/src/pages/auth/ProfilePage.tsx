import PlaceholderPage from "../../components/layout/PlaceholderPage";

// Member 2 (Auth & profiles): the current user's profile.
// Suggested: <Avatar>, display name, role <Badge variant="vet"> for vets, bio, edit form.
export default function ProfilePage() {
  return (
    <PlaceholderPage
      title="My profile"
      owner="Member 2"
      description="Avatar, display name, role badge, bio, and an edit-profile form."
    />
  );
}
