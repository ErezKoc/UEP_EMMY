import PlaceholderPage from "../../components/layout/PlaceholderPage";

// Member 2 (Auth & profiles): replace with the real login form.
// Suggested: email + password via <Input>, submit via <Button loading>, store the
// session in a SessionContext, redirect to /dashboard on success.
export default function LoginPage() {
  return (
    <PlaceholderPage
      title="Sign in"
      owner="Member 2"
      description="Login form with email and password, session state, and a redirect to the dashboard after signing in."
    />
  );
}
