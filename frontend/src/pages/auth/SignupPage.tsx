import PlaceholderPage from "../../components/layout/PlaceholderPage";

// Member 2 (Auth & profiles): replace with the real signup form.
// Must include the role choice: pet owner vs. veterinarian (backend User.role).
export default function SignupPage() {
  return (
    <PlaceholderPage
      title="Create an account"
      owner="Member 2"
      description="Signup form with name, email, password, and a role selection (pet owner or veterinarian)."
    />
  );
}
