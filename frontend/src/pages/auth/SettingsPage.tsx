import PlaceholderPage from "../../components/layout/PlaceholderPage";

// Member 2 (Auth & profiles): account settings.
export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      owner="Member 2"
      description="Account settings: email, password change, and notification preferences."
    />
  );
}
